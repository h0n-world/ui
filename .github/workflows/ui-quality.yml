name: UI quality

on:
    pull_request:
    push:
        branches: [main]

permissions:
    contents: read

concurrency:
    group: ui-quality-${{ github.ref }}
    cancel-in-progress: true

jobs:
    quality:
        name: Ubuntu quality
        runs-on: ubuntu-latest
        timeout-minutes: 25
        steps:
            - uses: actions/checkout@v6
            - uses: pnpm/action-setup@v6
            - uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: pnpm
            - run: pnpm install --frozen-lockfile
            - run: pnpm --filter @h0nio/icons check
            - run: pnpm --filter @h0nio/icons test
            - run: pnpm --filter @h0nio/icons build
            - run: pnpm --filter @h0nio/icons verify:package
            - run: pnpm --filter @h0nio/icons verify:consumer
            - run: pnpm --filter @h0nio/icons verify:size
            - run: pnpm --filter @h0nio/icons verify:tarball
            - run: pnpm --filter @h0nio/ui typecheck
            - run: pnpm --filter @h0nio/ui test
            - run: pnpm --filter @h0nio/ui build
            - run: pnpm --filter @h0n/ui-documentation agents:test
            - run: pnpm --filter @h0n/ui-documentation build

    browser:
        name: Ubuntu ${{ matrix.browser }}
        runs-on: ubuntu-latest
        timeout-minutes: 25
        strategy:
            fail-fast: false
            matrix:
                browser: [chromium, firefox, webkit]
        steps:
            - uses: actions/checkout@v6
            - uses: pnpm/action-setup@v6
            - uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: pnpm
            - run: pnpm install --frozen-lockfile
            - run: pnpm exec playwright install --with-deps ${{ matrix.browser }}
            - run: pnpm exec playwright test packages/ui/tests-visual/component-smoke.spec.ts packages/ui/tests-visual/component-interactions.spec.ts packages/ui/tests-visual/documentation-navigation.spec.ts packages/ui/tests-visual/icons-catalog.spec.ts packages/ui/tests-visual/table-geometry.spec.ts packages/ui/tests-visual/floating-performance.spec.ts packages/ui/tests-visual/runtime-hardening.spec.ts --config packages/ui/playwright.config.ts --project=${{ matrix.browser }}

    accessibility:
        name: Chromium accessibility
        runs-on: ubuntu-latest
        timeout-minutes: 25
        steps:
            - uses: actions/checkout@v6
            - uses: pnpm/action-setup@v6
            - uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: pnpm
            - run: pnpm install --frozen-lockfile
            - run: pnpm exec playwright install --with-deps chromium
            - run: pnpm exec playwright test packages/ui/tests-visual/accessibility.spec.ts --config packages/ui/playwright.config.ts --project=chromium

    windows-table:
        name: Windows Chromium Table geometry
        runs-on: windows-latest
        timeout-minutes: 20
        steps:
            - uses: actions/checkout@v6
            - uses: pnpm/action-setup@v6
            - uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: pnpm
            - run: pnpm install --frozen-lockfile
            - run: pnpm exec playwright install chromium
            - run: pnpm exec playwright test packages/ui/tests-visual/table-geometry.spec.ts packages/ui/tests-visual/runtime-hardening.spec.ts --config packages/ui/playwright.config.ts --project=chromium
