import type { H0CssSize, H0Orientation } from '../../types'
export type H0ScrollAreaProps = { orientation?: H0Orientation | 'both'; maxHeight?: H0CssSize; maxWidth?: H0CssSize; stableGutter?: boolean; fadeEdges?: boolean; ariaLabel?: string }
