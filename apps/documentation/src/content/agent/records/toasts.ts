import type { ComponentAgentRecordV1 } from '../schema.ts'

export const toastsAgentRecord = {
    schemaVersion: 1, component: 'H0Toasts', status: 'migrated', summary: 'Teleported toast stack that renders recent service items with placement, stacking, transitions, and dismissal delegation.',
    imports: { components: ['H0Toasts'], types: ['H0ToastService', 'H0ToastsProps'], styles: ['@h0nio/ui/style.css'] },
    api: { props: [{ name: 'maxVisible', type: 'number', default: '4', description: 'Maximum recent service items rendered in the stack.' }, { name: 'service', type: 'H0ToastService', default: 'Injected H0N toast service', description: 'Explicit service override for isolated stacks or tests.' }], events: [], slots: [], exposed: [], types: [] },
    useWhen: ['A toast service needs one application-level visual stack.', 'An isolated subtree supplies an explicit toast service.'], avoidWhen: ['Rendering an individual application-owned preview; use H0Toast.', 'Several stacks would consume the same service.'], accessibility: ['Render one polite live region per service.', 'Keep maxVisible bounded.', 'Ensure persistent items remain dismissible.'], styling: ['Configure placement through the service.', 'Treat stack transforms and transition classes as implementation details.'], responsive: ['Centered placements expand between viewport edges on small screens.', 'The stack reserves 16px mobile edge spacing.'], performance: ['Render one stack per service.', 'Keep maxVisible bounded and dispose manual services.'], examples: [{ key: 'components/toast/ServiceExample', purpose: 'Explicit service passed to a teleported stack.' }, { key: 'components/toast/CloseModesExample', purpose: 'Stack dismissal delegation for each close mode.' }], relatedComponents: ['H0Toast'],
} satisfies ComponentAgentRecordV1
